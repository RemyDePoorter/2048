package DesignPattern;

/**
 * Interface of an observer in an "Observer DP".
 *
 * @author Rémy
 */
public interface Observer {

    /**
     * Updates the observer when the subject has changed.
     */
    public void update();
}
